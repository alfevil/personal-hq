# Personal HQ 🚀

Личный штаб: мысли, проекты, бюджет. Telegram Mini App.

**Стек:** React + Vite + Tailwind CSS + Supabase + Recharts  
**Деплой:** Vercel (бесплатно)

---

## Установка и деплой: пошаговый гайд

### Шаг 1 — Настройка Supabase

1. Иди на [supabase.com](https://supabase.com) → создай аккаунт → **New Project**
2. Придумай название (например `personal-hq`), придумай пароль к БД, выбери регион (Frankfurt — ближайший)
3. Подожди ~1 минуту пока проект поднимется
4. Зайди в **SQL Editor** (левая панель) → **New Query**
5. Скопируй всё содержимое файла `supabase/schema.sql` и выполни (**Run** или Ctrl+Enter)
6. Убедись что таблицы создались: **Table Editor** → должны быть `thoughts`, `projects`, `transactions` и т.д.
7. Скопируй ключи: **Settings → API**:
   - `Project URL` → это `VITE_SUPABASE_URL`
   - `anon public` ключ → это `VITE_SUPABASE_ANON_KEY`

---

### Шаг 2 — Загрузка проекта на GitHub

1. Создай новый репозиторий на [github.com](https://github.com) (например `personal-hq`, приватный)
2. Скачай код проекта и загрузи в репозиторий:

```bash
# Если у тебя уже есть git
git init
git add .
git commit -m "init"
git remote add origin https://github.com/ТВОЙ_НИК/personal-hq.git
git push -u origin main
```

---

### Шаг 3 — Деплой на Vercel

1. Зайди на [vercel.com](https://vercel.com) → **Sign In with GitHub**
2. **Add New → Project** → выбери репозиторий `personal-hq`
3. Framework Preset автоматически определится как **Vite** ✅
4. В разделе **Environment Variables** добавь:
   - `VITE_SUPABASE_URL` = твой URL из шага 1
   - `VITE_SUPABASE_ANON_KEY` = твой ключ из шага 1
5. Нажми **Deploy** → подожди ~1 минуту
6. Vercel даст тебе URL вида `https://personal-hq-xxx.vercel.app` — **сохрани его**

---

### Шаг 4 — Создание Telegram бота

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Напиши `/newbot` → придумай название → придумай username (например `my_personal_hq_bot`)
3. Получишь токен бота — сохрани (он пригодится только если хочешь команды, для Mini App не обязателен)
4. Напиши `/newapp` → выбери своего бота
5. Укажи:
   - Title: `Personal HQ`
   - Description: `Мой личный штаб`
   - Photo: любая картинка 640x360
   - Web App URL: `https://personal-hq-xxx.vercel.app` (URL из шага 3)
6. Получишь ссылку на Mini App вида `https://t.me/my_personal_hq_bot/app`

**Альтернативно** — создай кнопку в боте:

```
/mybots → выбери бота → Bot Settings → Menu Button → Configure menu button
URL: https://personal-hq-xxx.vercel.app
```

---

### Шаг 5 — Локальная разработка (опционально)

```bash
# Установи зависимости
npm install

# Создай файл .env (скопируй из .env.example)
cp .env.example .env
# Заполни VITE_SUPABASE_URL и VITE_SUPABASE_ANON_KEY

# Запусти dev-сервер
npm run dev
# → http://localhost:5173
```

> ⚠️ В браузере без Telegram SDK будет работать в режиме dev — данные привяжутся к случайному ID из localStorage.

---

## Обновление приложения

Изменил что-то в коде → запушил в GitHub → Vercel автоматически передеплоит. Всё.

```bash
git add .
git commit -m "fix: что поправил"
git push
```

---

## Структура проекта

```
personal-hq/
├── src/
│   ├── lib/
│   │   └── supabase.js        # Supabase клиент + getUserId
│   ├── hooks/
│   │   ├── useThoughts.js     # Все операции с мыслями
│   │   ├── useProjects.js     # Проекты, этапы, задачи, заметки, ссылки
│   │   └── useBudget.js       # Транзакции, лимиты, статистика
│   ├── components/
│   │   ├── Navigation.jsx     # Bottom tab bar
│   │   └── Modal.jsx          # Sliding bottom sheet
│   ├── pages/
│   │   ├── Thoughts.jsx       # 🧠 Мысли с тегами
│   │   ├── Projects.jsx       # 📁 Список проектов
│   │   ├── ProjectDetail.jsx  # Детальная страница проекта
│   │   ├── Budget.jsx         # 💰 Бюджетка + аналитика
│   │   └── Settings.jsx       # ⚙️ Настройки
│   ├── App.jsx                # Роутинг по вкладкам
│   ├── main.jsx               # Entry point + TG init
│   └── index.css              # Tailwind + custom styles
├── supabase/
│   └── schema.sql             # SQL для создания таблиц
├── .env.example               # Шаблон переменных окружения
├── index.html
├── vite.config.js
├── tailwind.config.js
└── package.json
```

---

## Частые проблемы

**Приложение не загружается в Telegram:**
- Проверь что URL в BotFather точно совпадает с URL на Vercel
- URL должен быть HTTPS (Vercel даёт автоматически)

**Данные не сохраняются:**
- Проверь переменные окружения на Vercel (Settings → Environment Variables)
- Проверь что SQL схема была выполнена в Supabase

**Белый экран:**
- Открой DevTools → Console → смотри ошибки
- Скорее всего проблема с VITE_SUPABASE_URL или VITE_SUPABASE_ANON_KEY

**Ошибка "relation does not exist":**
- Заново выполни `supabase/schema.sql` в SQL Editor Supabase
